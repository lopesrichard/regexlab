const html=document.querySelector("html"),body=document.querySelector("body"),fonts={flower:"Indie Flower",courier:"Courier",roboto:"Roboto",slabo:"Slabo",times:"Times New Roman",ubuntu:"Ubuntu"},select=document.getElementById("fonts");function setOptions(){let options="";for(let font in fonts)options+=`<option value="${font}">${fonts[font]}</option>`;return options}select.innerHTML=setOptions(),select.addEventListener("input",function(){for(let font in fonts)body.classList.remove(font);body.classList.add(select.value)});